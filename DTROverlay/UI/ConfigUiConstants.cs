using System.Numerics;

namespace DTROverlay.UI;

internal static class ConfigUiConstants
{
    public static readonly Vector2 DefaultWindowSize = new(1000, 720);
    public static readonly Vector2 MinimumWindowSize = new(1000, 600);
}
