using System.Numerics;

namespace DTROverlay.UI;

internal static class ConfigUiConstants
{
    public static readonly Vector2 DefaultWindowSize = new(900, 500);
    public static readonly Vector2 MinimumWindowSize = new(900, 500);
}
