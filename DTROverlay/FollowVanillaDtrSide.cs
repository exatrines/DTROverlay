namespace DTROverlay;

public enum FollowVanillaDtrSide
{
    /// <summary>Plugin overlay sits to the left of the vanilla DTR bar.</summary>
    Left,

    /// <summary>Plugin overlay sits to the right of the vanilla DTR bar.</summary>
    Right,
}
